services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
    command: npm run dev -- --host

volumes:
  node_modules:
